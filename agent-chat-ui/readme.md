# agent-chat-ui

This UI container expects an langraph api hosting an agent.

In the compose file there are expected env variables to be set. If they are not set the UI will start first with the dialog for confirmation the defaults.
